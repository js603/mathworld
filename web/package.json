{
    "name": "mathworld-web",
    "version": "1.0.0",
    "description": "MathWorld 텍스트 RPG - 웹 버전",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist"
    },
    "dependencies": {
        "peerjs": "^1.5.2"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0",
        "typescript": "^5.3.3",
        "vite": "^5.0.10"
    }
}